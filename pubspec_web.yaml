name: anal_gis
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # Карты и геолокация (web совместимые версии)
  flutter_map: ^6.1.0
  geolocator: ^10.1.0
  geocoding: ^2.1.1
  
  # Навигация и маршруты
  flutter_polyline_points: ^2.0.0
  http: ^1.1.0
  
  # Локализация
  intl: ^0.20.2
  
  # Управление состоянием
  provider: ^6.1.1
  
  # Хранение данных (web совместимые)
  shared_preferences: ^2.2.2
  
  # UI компоненты
  material_design_icons_flutter: ^7.0.7296
  
  # Изображения и ассеты
  cached_network_image: ^3.3.0
  
  # Аутентификация (web совместимые)
  google_sign_in: ^6.2.1
  firebase_auth: ^4.17.8
  firebase_core: ^2.27.0
  crypto: ^3.0.3
  
  # Email отправка (web совместимая)
  mailer: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
